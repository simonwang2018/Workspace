python不像其他程序设计语言（如Java或者C语言）采用大括号“｛｝”分隔代码块，而是采用代码缩进和冒号“：”区分代码之间的层次。
说明：缩进可以使用空格或者Tab键实现。其中，使用空格时，通常情况下采用4个空格作为一个缩进量，而使用Tab键时，则采用一个Tab键作为一个缩进
量。通常情况下建议采用空格进行缩进。
在python中，对于类定义、函数定义、流程控制语句、异常处理语句等，行尾的冒号和下一行的缩进表示一个代码块的开始，而缩进结束，则表示一个代
码块的结束。
