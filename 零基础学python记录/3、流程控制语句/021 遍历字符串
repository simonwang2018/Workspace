使用for循环语句除了可以循环数值，还可以逐个遍历字符串，例如，下面的代码可以将横向显示的字符串转换为纵向显示：
string = '不要再说我不能'
print(string)                           # 横向显示
for ch in string:
    print(ch)                           # 纵向显示

说明：for循环语句还可以用于迭代（遍历）列表、元组、集合和字典等。
