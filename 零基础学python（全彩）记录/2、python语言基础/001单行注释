在python中，使用‘#’作为单行注释的符号。从符号#开始直到换行为止，#后面所有的内容都作为注释的内容，并被python编译器忽略。
语法如下：
# 注释内容

单行注释可以放在注释代码的前一行，也可以放在要注释代码的右侧。例如，下面的两种注释形式都是正确的。
第一种形式：
# 要求输入身高，单位为m
height=float(input('请输入您的身高：'))
第二种形式：
height=float(input('请输入您的身高：'))   # 要求输入身高，单位为m
